function simulateQrScan() {
    const scannedData = "paynow-redirect"; 
    if (scannedData === "paynow-redirect") {
        window.location.href = 'payment.html';
    }
}

setTimeout(simulateQrScan, 3000);